{
  "name": "todo-list-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build && cd ../server && npm install",
    "start": "npm run build && cd server && npm start"
  },
  "dependencies": {},
  "engines": {
    "node": "16.13.1",
    "npm": "8.3.0"
  }
}
